<!DOCTYPE html>
<html>
  <head> </head>
  <body>
    <ul>
      <% if (locals.users) {%> <% users.forEach(user => { %>
      <li>ID : <%= user.id %></li>
      <li>
        Name: <%= user.username %>
        <li>E-mail : <%= user.email %></li>
        <li>Age : <%= user.age %></li>
        <li>Bio : <%= user.bio %></li>
        <% }); %> <% } %>
      </li>
    </ul>
    <a href="/">Back to home</a>
  </body>
</html>
